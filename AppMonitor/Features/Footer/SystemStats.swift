// SystemStats lives in Core/Sampling as `SystemStatsSnapshot` — this stub file
// is kept as a placeholder so the folder structure mirrors the plan. If the
// footer needs derived/formatted state distinct from the snapshot, add it here.
